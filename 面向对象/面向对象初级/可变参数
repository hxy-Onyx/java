可变参数：可以接受不定数量的参数
语法：
返回类型 方法名(数据类型... 参数名) {
    // 方法体
}
例如，定义一个接受多个整数并返回它们总和的方法：
public int sum(int... numbers) {
    int result = 0;
    for (int num : numbers) {
        result += num;
    }
    return result;
}
注意：
a) 可变参数必须是方法中的最后一个参数
b) 可变参数可以接受 0 个或多个参数
c) 可变参数和数组的关系可变参数在方法内部实际上是作为数组处理的。所以可以像操作数组一样操作可变参数。
d) 传递数组给可变参数
e) 多个重载方法与可变参数的使用：如果一个类中有多个重载的方法，其中之一是可变参数方法，编译器会优先选择匹配精确参数的方法，而不是可变参数方法。
